import React from "react";

export default () => {
  return (
    <div
      style={{
        textAlign: "center",
        backgroundColor: "blue",
        height: "100vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <span style={{ textAlign: "center", color: "#fff", fontSize: 60 }}>
        react about.
      </span>
    </div>
  );
};
